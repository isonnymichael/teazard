/*
* This script is responsible for saving and loading the data of a game to an XML file using Unity's built-in XmlSerializer, which allows you serialize objects into xml files that can be read back later.  It also includes methods in this class used by other scripts such as StartAttack() & StopAttack().
*/
public static class PlayerDataStatic{
    public static PlayerData playerData;
}